package constants

// Loop variable names
const (
	LoopVariableIteration = "iteration"
	LoopVariableIndex     = "index"
	LoopVariableItem      = "item"
)

// Common error message templates (safe, no format strings)
const (
	ErrorInvalidRangeFormat = "invalid range format"
	ErrorInvalidStartValue  = "invalid start value in range"
	ErrorInvalidEndValue    = "invalid end value in range"
	ErrorInvalidCountFormat = "invalid count format"
)
