testcase: "User Update"
description: "A test that would be skipped if fail-fast triggers."
steps:
  - name: "Log update"
    action: log
    args: ["User updated!"]
  - name: "Assert true"
    action: assert
    args:
      - 2
      - "=="
      - 2
      - "Basic numeric equality should work" 